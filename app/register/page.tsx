import { Button } from "@/components/ui/button";

export default function Register() {

    return (
        <div className="register">
            <p>Not currently accepting registrations</p>
        </div>
      );

}